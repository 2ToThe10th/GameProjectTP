#ifndef GAME_ENUMS_H
#define GAME_ENUMS_H

enum Player {
    Me,
    Opponent
};

enum Direction {
    Up,
    Left,
    Right,
    Down
};

enum Race {
    Water,
    Fire,
    Earth,
    Air
};

enum Resource {
    Nothing,
    Gold,
    Silver,
    Wood
};

#endif //GAME_ENUMS_H
